# SwiftPay — ledger-service CI
#
# Independent pipeline for this service only. Path filter scopes triggers to
# ledger-service changes so unrelated commits don't fan out work here.
#
# Pipeline: assemble → test + coverage → docker build (verify-only; no push).

name: ledger-service CI

on:
  push:
    paths:
      - 'ledger-service/**'
      - '.github/workflows/ledger-service.yml'
  pull_request:
    paths:
      - 'ledger-service/**'
      - '.github/workflows/ledger-service.yml'
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: ledger-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  SERVICE_DIR: ledger-service

jobs:
  build-test:
    name: Build · Test · Coverage
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ env.SERVICE_DIR }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Temurin JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - name: Set up Gradle (with caching)
        uses: gradle/actions/setup-gradle@v4
        with:
          build-root-directory: ${{ env.SERVICE_DIR }}
          cache-read-only: ${{ github.ref != format('refs/heads/{0}', github.event.repository.default_branch) }}
          gradle-home-cache-cleanup: true

      - name: Assemble (compile + bootJar without running tests)
        run: ./gradlew assemble --no-daemon

      - name: Test + JaCoCo coverage
        run: ./gradlew test jacocoTestReport --no-daemon

      - name: Upload JUnit results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: junit-ledger-service
          path: ${{ env.SERVICE_DIR }}/build/test-results/test
          retention-days: 14

      - name: Upload JaCoCo HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-html-ledger-service
          path: ${{ env.SERVICE_DIR }}/build/reports/jacoco/test/html
          retention-days: 14

      - name: Upload JaCoCo XML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-xml-ledger-service
          path: ${{ env.SERVICE_DIR }}/build/reports/jacoco/test/jacocoTestReport.xml
          retention-days: 14

      - name: Upload boot JAR
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ledger-service-jar
          path: ${{ env.SERVICE_DIR }}/build/libs/ledger-service-*-SNAPSHOT.jar
          retention-days: 14

  docker-build:
    name: Docker image build (verify-only)
    runs-on: ubuntu-latest
    needs: build-test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image (no push)
        uses: docker/build-push-action@v6
        with:
          context: ./ledger-service
          push: false
          load: false
          tags: swiftpay/ledger-service:${{ github.sha }}
          cache-from: type=gha,scope=ledger-service
          cache-to: type=gha,mode=max,scope=ledger-service